<%= provide(:title, 'Positions') %>
<h1><%= yield :title %></h1>

<%= link_to 'Add Position', new_admin_position_path %>
<table class="table table-hover">
	<thead>
		<tr>
            <th>Title</th>
            <th>Status</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
    <% @positions.each do |data| %>
    <tr>
    	<td><%= data.title %></td>
        <td><%= data.status.title %></td>
        <td>
            <%= link_to "Show", admin_position_path(data) %>
            <%= link_to "Edit", edit_admin_position_path(data) %>
            <%= link_to "Delete", [:admin, data], method: :delete, data: {confirm: "Are you sure?"} %>  
        </td>
    </tr>
    <% end %>
    </tr>
  </tbody>
</table>

<%= will_paginate @positions %>